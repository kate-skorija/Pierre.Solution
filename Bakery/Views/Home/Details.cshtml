@{
  Layout = "_Layout";
}

@using Bakery.Models;
@Html.Partial("Nav")

<div class="splash">
  <h1>Pierre's Bakery</h1>
  <div class="row">
    <div class="col col-sm-3">
      <ul><h2>Treats</h2>
        @foreach (Treat treat in ViewBag.Treats)
        {
          <li>@Html.ActionLink($"{treat.Name}", "Details", "Treats", new { id = treat.TreatId })</li>
        }
      </ul>
      <button class="btn btn-dark">@Html.ActionLink("Add Treat", "Create", "Treats")</button>
    </div>

    <div class="col col-sm-3">
      <ul><h2>Flavors</h2>
        @foreach (Flavor flavor in ViewBag.Flavors)
        {
          <li>@Html.ActionLink($"{flavor.Name}", "Details", "Flavors", new { id = flavor.FlavorId })</li>
        }
      </ul>
      <button class="btn btn-dark">@Html.ActionLink("Add Flavor", "Create", "Flavors")</button>
    </div>
  </div>
</div>